
<h1>NAME:<%= @user.name %></h1>
<p>メールアドレス: <%= @user.email %></p>
<p>
  <strong>Image:</strong>
  <%= image_tag @user.image.url, style:'width:200px'   %>
</p>
<button class="btn btn-primary" >
<%= link_to "編集", edit_user_path(current_user.id) %></button>
<button class="btn btn-primary">
<%= link_to "お気に入り", favorites_path(current_user.id) %></button>